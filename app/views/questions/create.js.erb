// closes the modal, cleans the form values
// adds the new row in the table and gives the message

$('#frm').modal('hide');
$('#t').val('');
$('#b').val('');
$('table#qta tbody').append("<%= j render @question %>")
alert('New Question Saved');